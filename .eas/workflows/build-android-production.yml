name: Android production build

# Manual trigger only — see comment in build-ios-production.yml.
on:
  workflow_dispatch: {}
  # push:
  #   branches: ['main']

jobs:
  build_android:
    name: Build Android
    type: build
    params:
      platform: android
      profile: production
